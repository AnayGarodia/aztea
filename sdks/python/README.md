# Legacy SDK Shim

`sdks/python/` is deprecated.

The canonical Python SDK now lives in:

```text
sdks/python-sdk/
```

This tree remains only as a compatibility shim for older local imports. New SDK work belongs in `sdks/python-sdk/`.
