"""Group records by category and return summary statistics per group."""


def handler(records: list[dict[str, int]]) -> dict[str, list[dict[str, int]]]:
    """Bucket numeric records by their `category` key."""
    return {}
